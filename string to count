#include <stdio.h>
void main() 
{
	char a[100];
	int count =0,i = 0;
	scanf("%s",a);
	while(a[i])
	{
	    count += 1;
	    if(a[i]!= a[i+1])
	    {
	        printf("%d%c",count,a[i]);
	        count = 0;
	    }
	    i++;
	}
}
input:
AAABBCA
output:
3A2B1C1A
